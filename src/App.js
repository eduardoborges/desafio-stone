import React from 'react';
import { hot } from 'react-hot-loader';

function App() {
  return (
    <div className="App">
      <h1>Olá pessoas!</h1>
    </div>
  );
}

export default hot(module)(App);
